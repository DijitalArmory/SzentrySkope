'''from data.net_scan_constants import *
from info.net_info import (ipv4, network)

network_str = str(network)

# scan commands
scan_top_localhost_ports_list = [SCAN, Pn, ipv4]
scan_all_localhost_ports = [SCAN, Pn, ipv4, ALL_PORTS]
scan_top_subnet_ports = [SCAN, Pn, network_str]
scan_all_subnet_ports = [SCAN, Pn, network_str, ALL_PORTS]'''